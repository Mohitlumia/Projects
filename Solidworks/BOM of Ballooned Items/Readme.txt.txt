this solidworks macro runs in drawing (.slddrw) files and remove rows form BOM which aren't ballooned.

it is helpful to get BOM of visible items only.

Note:

1. it keeps top header by default.